import { Glance } from "../utils/svgs"

const Header = () => {
    return (
        <header className="flex p-5 shadow-xs border-b border-gray-200 fixed w-full">
            <Glance/>
        </header>
    )
}

export default Header